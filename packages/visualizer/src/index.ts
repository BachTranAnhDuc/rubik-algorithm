// Default barrel — re-exports the SSR-safe surface for general use. For
// strictly RSC-safe imports prefer `@rubik/visualizer/ssr`; for the future
// 3D Visualizer prefer `@rubik/visualizer/client` (lazy-loaded).

export * from './ssr'
